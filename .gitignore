# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build

# generated pdfs (versioned, kept locally)
/pdfs

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vscode/settings.json

# claude - share skills/rules/commands, but not personal settings
.claude/settings.local.json
# machine-local: skills this repo opts out of (see .agents/scripts/init.sh)
.agents-ignore

# python venv for scripts/check-cv-pdf
/.venv-ats

# preview recording; published as a release asset by generate-gif.yml, never
# committed (it is ~6 MB and the recording differs on every run)
/cv-preview.gif
