name: Generate demo gif

on:
  push:
    branches:
      - master # Trigger the workflow on pushes to master

jobs:
  generate-gif:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate GIF from Website
        uses: PabloLec/website-to-gif@2.1.1
        with:
          url: "https://mrtysn.github.io/cv/"
      - name: Commit GIF to Repository
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"
          git add .
          git commit -m 'Automatically generated GIF'
          git push
